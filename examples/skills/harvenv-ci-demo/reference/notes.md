# Notes

A second file, one directory down. Its only job is to make the skill a tree
rather than a file, so the Store's content hash and the CI recipe's
"byte-identical" check have some structure to cover.
