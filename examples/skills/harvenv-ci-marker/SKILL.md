---
name: harvenv-ci-marker
description: Second marker skill for harvenv's own CI recipe example. It does nothing and is never invoked — it exists so the sample Manifest declares a set rather than a single entry.
---

# harvenv-ci-marker

A single file, from the same repository and the same commit as
[harvenv-ci-demo](../harvenv-ci-demo/SKILL.md) but a different subdirectory —
so the sample Manifest locks two distinct Store entries, and "the session
contains exactly what the Manifest declares" is a claim about a set with more
than one member in it.
