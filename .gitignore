.DS_Store
*.log
node_modules/
dist/

# personal overlay + generated environment (once harv exists)
harvenv.local.toml
.harv/

# written by `harv sync` into any project below here — generated, not authored.
# `harvenv.lock` is the opposite: it is committed.
.claude/skills/
.claude/.harv-materialized.json
